
+ gactions: is the command line interface that you use to preview, simulate, and publish an action package. If you encouter errors with the bundled binary, use the gactions from the official website. GAS includes this just for convenience.
+ ngrok: Introspected tunnels to localhost. Allows Google’s server to access your local action (while in dev mode)
+ nodemon: Monitor for any changes in your node.js application and automatically restart the server.